version: '3.5'

services:
  report-ci:
    build: ./
    networks:
      router:
        aliases:
          - report-ci
    volumes:
      - ./:/var/www/html

networks:
  router:
    external:
      name: router
